# SF-Panel — نسخه اصلاح‌شده

## چه چیزهایی درست شده

### ربات تلگرام (پوشه `telegram/`)
- رفع باگ کد تخفیف (دیگر موقع پرداخت پاک نمی‌شود)
- پرداخت atomic (جلوگیری از دوباره‌خرید و Race Condition)
- ذخیره صحیح `tg_id` در پنل هنگام ساخت اکانت
- ارسال کانفیگ کامل + لینک ساب + QR Code بعد از خرید
- مدیریت بهتر state و قفل thread-safe
- پیام راهنما وقتی `/panel` می‌زنی ولی `tg_admins` خالی است
- نام فایل `panel_link.py` (سازگار با لینوکس)

### پنل اصلی
- `requirements.txt`: اضافه شدن `Pillow` (برای QR)
- پیشنهاد تغییر منطق seed رمز ادمین (دیگر پسورد ثابت `Sina990` نباشد)

---

## نحوه نصب روی گیت‌هاب / Railway

### ۱. جایگزینی پوشه ربات
1. پوشه `telegram` فعلی ریپو را **کاملاً حذف** کن.
2. محتویات پوشه `telegram` داخل این پکیج را کپی کن جای آن.

### ۲. آپدیت requirements.txt
فایل `requirements.txt` ریشه پروژه را با نسخه داخل این پکیج عوض کن (یا فقط خط `Pillow>=10.0` را اضافه کن).

### ۳. (اختیاری ولی توصیه‌شده) امنیت ادمین
در `app.py` بخش `DEFAULT_ADMIN_USER` / `DEFAULT_ADMIN_PASS` و تابع `seed_from_env` را با نسخه داخل `patches/app_seed_fix.py` عوض کن  
یا حداقل این Environment Variableها را در Railway ست کن:

```
ADMIN_USER=yourname
ADMIN_PASS=یک_رمز_قوی
TG_BOT_TOKEN=توکن_ربات
TG_ADMIN_IDS=آیدی_عددی_تلگرام_تو
PUBLIC_DOMAIN=دامنه_یا_آدرس_railway_تو
```

### ۴. تنظیم ادمین ربات
بعد از بالا آمدن پنل:
- وارد پنل وب شو
- Settings → فیلد `tg_admins`
- آیدی عددی تلگرامت را بگذار (مثلاً `123456789`)
- ذخیره کن

یا از Environment Variable `TG_ADMIN_IDS` استفاده کن.

### ۵. Redeploy
روی Railway یک Redeploy بزن.

---

## چک‌لیست بعد از دیپلوی

- [ ] `/start` در ربات جواب می‌دهد
- [ ] `/panel` پنل ادمین را باز می‌کند
- [ ] خرید اشتراک → کانفیگ + ساب + QR می‌آید
- [ ] افزایش موجودی با رسید کار می‌کند
- [ ] رمز ادمین وب عوض شده

---

## نکات مهم

1. **آیدی تلگرام** را فقط در `tg_admins` (پنل وب یا env) بگذار، نه داخل کد.
2. روی Railway فقط پروتکل‌های سازگار با یک پورت (مثل VLESS+WS) پایدار هستند.
3. اگر QR نیامد، مطمئن شو `Pillow` در requirements هست و بیلد دوباره گرفته شده.
